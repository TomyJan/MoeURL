# 实施文档

本目录用于保存 MoeURL 的实施计划、阶段任务和验收记录。

实施文档应遵循以下原则：

- 先确认产品范围，再拆分开发任务。
- 以 v0.0.1 基础可用闭环为当前优先目标。
- 不把远期能力直接纳入当前版本实现。
- 对远期能力只保留必要的数据模型、接口边界或扩展点。
- 每份实施计划都应明确目标、范围、任务顺序和验收标准。

建议实施计划按版本命名，例如：

```text
v0.0.1-plan.md
v0.1.0-plan.md
```
