# v0.3.0 受保护短链访问详细实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 `superpowers:executing-plans` 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 在保留 v0.2.0 直接/中间页行为的前提下，实现密码保护、数据库一致失败限流和短期访问授权。

**架构：** 复用现有短链访问服务和 `/go/{slug}` 页面。密码哈希、失败窗口和授权令牌使用 PostgreSQL 共享状态；Handler 只负责业务错误和 Cookie 边界，Service 负责访问条件与事务。

**技术栈：** Go 1.25、Chi、SQLC、Goose、PostgreSQL、Argon2id、Vue 3、Vuetify 4、TanStack Query、Vitest、Playwright。

---

## 文件边界

- 创建：`migrations/00006_short_link_password.sql`、`docs/product/scope-v0.3.0.md`、`docs/specs/2026-08-04-v0.3.0-protected-link-access-design.md`、本计划和实现文档。
- 修改：`queries/short_link.sql`、`internal/db/sqlc` 生成文件、`internal/permission/permission.go`、`internal/shortlink/{model,errors,service,redirect_service,redirect_handler,handler}.go`、`internal/http/router.go`、前端短链模型/API/创建/设置/公共访问页面/i18n、E2E 和对应测试。
- 验证：Go 全量测试与覆盖率、Vitest 全量测试与覆盖率、lint、typecheck、build、Playwright E2E、SQLC 零漂移、`git diff --check`。

## 任务 1：版本文档与基线同步

- [x] 编写范围、功能规格、计划、任务清单和空白验收模板，明确 v0.3.0 只包含受保护短链访问。
- [x] 更新 `docs/README.md`、`docs/product/overview.md`、`docs/product/roadmap.md`、`AGENTS.md`、技术决策和技术基线，加入密码字段不入响应、短期授权和数据库限流约定。
- [x] 扫描范围、规格和计划文档中的占位词，预期无输出。

## 任务 2：数据库、权限和 SQLC 契约

- [x] 在 `migrations/00006_short_link_password.sql` 增加密码字段、失败窗口字段、`password_updated_at` 和 `short_link_access_grant` 表；Down 按依赖顺序删除授权表再删除字段。
- [x] 在 `internal/db/migration_test.go`、`internal/db/sqlc_test.go` 增加既有数据默认未保护、创建/更新/读取密码字段、授权过期和密码更新时间失效测试；集成执行待数据库环境可用后完成。
- [ ] 在 `queries/short_link.sql` 增加创建、更新、读取密码字段和授权/失败窗口查询，并在 SQLC 环境可用后重新生成和完成零漂移核对；当前生成代码已通过 Go 编译检查，但本机无法连接 `proxy.golang.org` 安装或运行 SQLC。
- [x] 更新 `internal/permission/permission.go` 与 migration 内置权限，验证 `guest` 不获得新权限、`user`/`admin` 获得新权限。
- [ ] 运行 `go test ./internal/db ./internal/permission -count=1` 和 SQLC 零漂移检查。

## 任务 3：后端密码配置与解锁服务

- [x] 在 `internal/shortlink/service_test.go` 增加权限、密码长度、显式清除、保留未提供字段的测试。
- [x] 在 `internal/shortlink/redirect_test.go` 增加错误密码、第五次限流、成功清零、授权过期和密码变更吊销旧授权测试。
- [x] 在 `internal/shortlink/model.go` 增加 `PasswordInput`、`PasswordEnabled` 和 `AccessGrant`，服务端复用 `auth.HashPassword`/`VerifyPassword`，不把哈希映射到公开模型。
- [x] 扩展 `createAccessConfig`/`updateAccessConfig` 的权限和显式模式处理；密码省略保持原值，`never` 清除。
- [x] 实现 `Unlock`：事务内锁定短链行，执行状态/过期检查、失败窗口、Argon2id 比较、令牌生成和哈希落库。
- [x] 扩展 `Open`、`Preview`、`Continue` 使用密码条件；同源 `/go/{slug}/preview` 复用有效授权，Continue 验证短期授权后才返回目标。
- [x] 运行短链定向测试，确认失败测试先红后绿，并保持事件写入失败不阻断响应。

## 任务 4：Handler、路由和 Cookie 边界

- [x] 在 `internal/shortlink/handler_test.go` 增加 unlock 成功、错误、限流和 HTTP 200 业务错误断言。
- [x] 在 `internal/shortlink/redirect_handler_test.go` 增加受保护 Open/Preview/Continue 和 Cookie 作用域断言。
- [x] 实现 `RedirectHandler.Unlock`，只设置 `HttpOnly`、`SameSite=Lax`、生产环境 `Secure`、15 分钟 TTL、`/go/{slug}` Path 的 Cookie。
- [x] 更新 `RedirectPort`、`internal/http/router.go` 和固定路由测试；保持 `/go` 优先于短码。
- [ ] 运行 `go test ./internal/shortlink ./internal/http -count=1`。

## 任务 5：前端配置表单与公共密码页

- [x] 在实体 API、模型和共享校验中增加密码输入、`passwordEnabled`、预览 `requiresPassword` 和 unlock API 测试。
- [x] 在 `ShortLinkCreatePanel.vue` 和 `ShortLinkSettingsDialog.vue` 先补权限矩阵、空值保留、清除密码和错误状态测试。
- [x] 实现密码开关和输入框；无权限隐藏，开启后空值不提交，显式关闭提交 `mode: never`。
- [x] 在 `RedirectPage.vue` 补充密码页加载、错误、成功解锁、计时器清理和卸载竞态测试；实现 unlock 请求，成功后进入原有倒计时/Continue 流程；移动布局由 E2E 任务覆盖。
- [x] 补齐中英文 i18n，确保不会渲染哈希、目标 URL 或内部错误。
- [x] 运行相关 Vitest、lint 和 typecheck。

## 任务 6：E2E 与发布验收

- [x] 扩展 `web/e2e/v0.2.0-link-experience.spec.ts`，真实创建受保护短链，验证错误密码、成功跳转、访问量和旧授权失效；运行待 Docker 环境可用后完成。
- [ ] 在 `1280 x 720` 与 `390 x 800` 视口验证密码页、设置对话框和中间页无重叠/横向溢出；不得使用固定等待。
- [ ] 运行 `go test ./...`、`go vet ./...`、后端覆盖率门禁、`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm test:coverage`、`pnpm build`、`pnpm test:e2e`。
- [x] 更新 `docs/implementation/v0.3.0-acceptance.md`，只勾选有命令输出支撑的项目，记录 Docker/数据库环境阻断。
- [ ] 执行 `git diff --check`、`git status --short --branch`，按原子边界创建本地提交；不执行 push、merge、pull。
