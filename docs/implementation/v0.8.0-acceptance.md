# v0.8.0 多域名管理验收清单

状态：功能实现、本地门禁与分支 review 已完成；目标 CI、race、安全扫描、公网 DNS/TLS、隔离恢复与生产发布验收待补。

## 功能与安全

- [x] 仅管理短链域名；系统访问域名和个人/用户组默认值没有开放入口。
- [x] 默认与显式创建分别检查对应权限及 `domain_user_group` 授权，`guest` 不获创建能力。
- [x] 管理写操作校验 `admin:access`、`domain:manage`、乐观并发与引用保护。
- [x] 当前全局默认域名始终启用；切换默认与配置镜像在同一事务中完成。
- [x] 所有公开访问路径匹配实际 Host 和关联域名启用状态；错误 Host、停用域名按不存在处理。
- [x] 已发布短链 URL 不随默认切换变化，停用后重启可恢复原地址。
- [x] 管理页与创建选择器具备加载、错误、空状态、确认、冲突草稿与双语文案。

## 数据与本地门禁

- [x] `00013` Up、回填、Down、再次 Up 的数据库测试通过。
- [x] SQLC 连续两次生成结果一致（阶段切片记录）。
- [x] `go test ./... -count=1`、`go vet ./...` 与既定目标文件集合的后端 100% 覆盖率复验。
- [x] Node.js 26.3.0、pnpm 11.5.0 下最终 `corepack pnpm test:coverage`：45 个文件、564 个测试通过，四项 100%；`corepack pnpm lint`、`corepack pnpm typecheck`、`corepack pnpm build` 退出码均为 0。前端切片 `b786475` 提交前为 561 个测试。
- [x] v0.8.0 双 Host Playwright 场景由管理员授权、普通 `user` 组成员创建短链，并确认隔离 Compose 清理。
- [x] Playwright 全量和 Compose smoke 通过。
- [x] 分支 diff、迁移回滚、权限与部署边界已 review；收尾差异已纳入本地提交，未将仅有 Windows 行尾状态变化的 SQLC 文件作为生成内容提交。

## 目标环境门禁

- [ ] Node.js 26.x 目标 CI：记录提交 SHA、运行链接、版本和通过结果。
- [ ] Linux `go test -race ./... -count=1`、安全扫描与隔离备份恢复：记录可追溯运行证据。
- [ ] 公网每个启用域名的 DNS、可信 HTTPS、真实代理 Host（含非默认端口）、错误 Host、停用/恢复测试通过。
- [ ] v0.6.0 与 v0.7.0 未完成的目标环境证据已独立补齐，才能另行判断生产发布资格。

## 本地验证记录（2026-09-14）

- 工作目录：仓库根目录 `D:\Dev\TomyJan\MoeURL`；前端命令在 `web/`。Go `go env GOVERSION=go1.26.8`，`GOTOOLCHAIN=auto`；Node.js `v26.3.0`，pnpm `11.5.0`；Docker Engine `29.5.2`。
- 后端阶段测试：`go test ./... -count=1` 在公开访问切片完成时退出码 0；`go test ./internal/http ./internal/app -count=1` 在前端切片完成时退出码 0。最终全量复验另列，不继承阶段结果。
- 前端切片：`pnpm test:coverage` 45/45 文件、561/561 测试通过，Statements/Branches/Functions/Lines 均 100%；`pnpm lint`、`pnpm typecheck`、`pnpm build` 退出码 0。Node.js 26.3.0 的目录置于当前命令 `PATH` 首位，并通过该版本 `node.exe` 调用 pnpm 11.5.0。
- 后续本地复验（同一工作区、尚未对应最终提交 SHA）：`go test ./... -count=1`、`go vet ./...` 均退出码 0；`go test -p=1 -count=1 -coverprofile=coverage.out` 覆盖技术基线所列包并包含 `./internal/domain`，`node --test scripts/go-coverage-threshold.test.mjs` 10/10 通过，`node scripts/go-coverage-threshold.mjs coverage.out 100 --include-from=scripts/go-coverage-targets.txt --exclude-blocks-from=scripts/go-coverage-excluded-blocks.txt` 输出 100.00%。该阈值只统计目标文件清单；整个 `internal/domain` 包在 Go 原始报告中为 68.5%，不可等同为全包 100%。
- 前端复验：在 `web/` 以 Node.js 26.3.0、pnpm 11.5.0 执行 `pnpm test` 与 `pnpm test:coverage`，均为 45/45 文件、561/561 测试通过；覆盖率 Statements 2033/2033、Branches 1302/1302、Functions 684/684、Lines 1836/1836，四项均 100%。`pnpm lint`、`pnpm typecheck`、`pnpm build` 均退出码 0。
- 集成复验：`pnpm test:e2e --grep multi-domain` 2/2 通过，`pnpm test:e2e` 12/12 通过；独立 Compose project 的容器、网络和卷由 Playwright teardown 清理。Windows Git Bash 执行 `scripts/compose-smoke.sh --config-only` 和完整 `scripts/compose-smoke.sh` 均输出 assertions passed；SQLC 使用 `sqlc/sqlc:1.30.0` 连续生成两次，`git diff --exit-code -- internal/db/sqlc` 退出码 0，无新增未跟踪生成文件。Docker Engine 为 29.5.2；Go 有效工具链 `go1.26.8`，`GOTOOLCHAIN=auto`。
- 本段记录的是提交前的本地验证，不能代替目标提交的远程 CI 结果。远程 CI、Linux race、安全扫描、真实 DNS/TLS 和生产恢复仍待目标环境证据。

## 收尾复验

- `go test ./... -count=1` 和 `go vet ./...` 退出码 0；默认域名重复设默认时版本递增且旧版本冲突的真实数据库测试通过。收尾复审新增的「未被引用的默认域名地址与旧设置镜像同步」用例已先失败再通过；相关全量 Go 门禁在该最后修复后重新执行。
- `go test -p=1 -count=1 -coverprofile=coverage.out` 覆盖技术基线的业务包并包含 `./internal/domain`；覆盖率校验脚本 10/10，通过目标文件集合 100.00%。`internal/domain` 包本次原始语句覆盖率为 68.9%，不作为目标文件集合的同义指标。
- Node.js 26.3.0 下，原收尾复验 `pnpm test:coverage` 为 45/45 文件、562/562 用例；补充 CI 包集合与代理 Host 文档测试后，锁定 pnpm 11.5.0 的 `corepack pnpm test:coverage` 为 564/564。Statements 2042/2042、Branches 1310/1310、Functions 690/690、Lines 1842/1842，四项均为 100%；`corepack pnpm lint`、`corepack pnpm typecheck`、`corepack pnpm build` 退出码 0。本机裸 `pnpm` 为 11.23.0，不作为锁定版本的证据。
- `pnpm test:e2e --grep v0.8.0` 2/2、`pnpm test:e2e` 12/12，通过单 worker 顺序运行以避免其他场景修改共享内置组权限。`user` 组成员在独立浏览器上下文创建并验证双 Host 链路；两次运行的隔离 Compose project 容器、卷和网络已清理。
- 原有部署配置测试对 Playwright worker 数量的断言不涉及冷构建超时行为，删除该脆弱断言，保留 `600_000` 超时和 setup 项目依赖验证。SQLC 查询未在收尾修复中改变，`git diff --exit-code -- internal/db/sqlc` 退出码 0；Git Bash 运行 `scripts/compose-smoke.sh --config-only` 和完整脚本，分别输出配置与业务断言通过。
- 2026-09-15（Asia/Singapore）使用锁定的 pnpm 11.5.0 再次执行 `corepack pnpm test:e2e`，12/12 通过；Playwright teardown 删除本次 `moeurl-e2e-8080-000a7f0bb669` project 的容器、网络及 PostgreSQL 卷。上述均为本地证据，不代表生产发布门禁通过。
- 覆盖率目标清单加入 `internal/domain/validation.go` 后，发现 CI 的 Go 测试包列表尚未包含 `./internal/domain`。新增读取目标清单的 CI 契约测试先失败，再将该包加入 workflow、README 和技术基线后通过；按修正后的 CI 包集合生成覆盖率报告，校验脚本 10/10、目标文件集合 100.00%。目标环境 CI 仍须在提交后取得运行证据。
- 复审 Caddy 配置发现 `{http.request.host}` 不能保留非默认端口；改用 `{hostport}`，与 Nginx 的 `$http_host` 一同由文档配置测试约束。真实公网代理、DNS/TLS 与非默认端口访问仍待目标环境验证。
- 完整分支复审发现的 CI 包集合与 Caddy Host 示例问题均已修正；按更新后的文档、代码与测试复验未发现新的可证实阻断。提交后的远程 CI 结果仍须另行取得并记录 SHA 与运行链接。
