# v0.2.0 任务级实施清单

> 未执行的项目不得标记为完成。

## 1. 文档与范围

- [x] 盘点路线图、稳定规格、现有实现和生产质量缺口。
- [x] 新增 v0.2.0 产品范围与技术设计。
- [x] 同步文档中心、产品总览、路线图和稳定规格。
- [ ] 新增详细实现计划并完成规格覆盖、占位符和类型一致性自检。

## 2. 数据库与权限

- [ ] 先编写 migration 与 SQLC 失败测试。
- [ ] 新增短链访问配置 migration，并验证升级与回滚。
- [ ] 为内置 `user`、`admin` 用户组追加新增权限，为 `guest` 保持无权限。
- [ ] 扩展短链 SQLC 创建、读取、列表和更新查询。
- [ ] 运行 `go test ./internal/db ./internal/permission ./internal/system -count=1`。

## 3. 后端访问配置

- [ ] 先编写创建、编辑、权限、模式、倒计时和过期时间失败测试。
- [ ] 扩展短链模型、Service、Handler 和错误码。
- [ ] 保证状态启停请求不会清空访问配置。
- [ ] 覆盖个人所有权、管理员全站权限和基础设施错误。
- [ ] 运行 `go test ./internal/shortlink ./internal/http -count=1`。

## 4. 公共中间页访问链路

- [ ] 先编写初次打开、公开预览、继续跳转和过期拦截失败测试。
- [ ] 新增公开预览 API、`/go/{slug}` 和 `/go/{slug}/continue`。
- [ ] 同步固定路由优先级和 `go` 保留短码。
- [ ] 确认只有最终目标跳转成功写出后记录访问量。
- [ ] 覆盖倒计时期间禁用、删除和过期的二次检查。
- [ ] 运行 `go test ./internal/shortlink ./internal/http ./internal/event -count=1`。

## 5. 前端访问配置

- [ ] 先编写前端 API、创建组件和设置对话框失败测试。
- [ ] 扩展短链 TypeScript 模型和 API 契约。
- [ ] 在创建组件增加权限化高级设置。
- [ ] 新增共享访问配置对话框，并接入我的短链与全站短链。
- [ ] 展示跳转模式和过期状态，保留移动端可用性。
- [ ] 运行相关 Vitest、lint 和类型检查。

## 6. 中间页与二维码

- [ ] 安装并锁定 `qrcode` 与类型依赖。
- [ ] 先编写中间页、二维码和路由失败测试。
- [ ] 新增 `/go/:slug` 公共中间页及完整页面状态。
- [ ] 新增二维码预览和下载工具，并接入创建结果与两个列表。
- [ ] 使用路由级动态导入隔离统计、二维码和中间页依赖。
- [ ] 运行相关 Vitest、覆盖率和生产构建。

## 7. E2E 与发布收尾

- [ ] 修复退出登录 E2E 异步竞态并验证稳定复现。
- [ ] 新增中间页、继续跳转、过期拦截、配置编辑和二维码 E2E。
- [ ] 验证 1280 x 720 与 390 x 800 视口无重叠和布局溢出。
- [ ] 运行后端全量测试、`go vet` 和 100% 覆盖率门禁。
- [ ] 运行前端 lint、类型检查、单测、100% 覆盖率、构建和 Playwright E2E。
- [ ] 运行 `git diff --check`，完成最终规格与代码质量审查。
- [ ] 更新验收记录、`AGENTS.md` 与当前版本文档并提交。
