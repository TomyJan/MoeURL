# v0.0.4 验收清单

> 本文档用于 v0.0.4 开发完成后的功能和体验验收。未经验证的项目不得标记为完成。

## 1. 验收原则

- 优先验证短链访问基础统计闭环。
- 同时回归 v0.0.3 控制台列表体验和 v0.0.2 短链管理能力。
- 不验收 v0.0.4 明确暂不实现的统计分析页、访问明细和维度统计。
- 自动化验证应记录命令、时间和结果。
- 手工验证应记录环境、步骤和结果。

## 2. 环境验收

- [x] `go test ./...` 通过。
- [x] `go vet ./...` 通过。
- [x] 后端覆盖率门禁通过。
- [x] `cd web && pnpm lint` 通过。
- [x] `cd web && pnpm typecheck` 通过。
- [x] `cd web && pnpm test` 通过。
- [x] `cd web && pnpm test:coverage` 通过。
- [x] `cd web && pnpm build` 通过。
- [x] `git diff --check` 通过。

## 3. 文档验收

- [x] 文档中心列出 v0.0.4 范围、计划、任务和验收清单。
- [x] 产品总览说明当前优先目标是 v0.0.4 基础统计前置。
- [x] 路线图在 v0.0.3 和 v0.1.0 之间列出 v0.0.4。
- [x] 实施文档入口列出 v0.0.4 实施文档。
- [x] v0.0.4 文档没有把完整统计页、访问明细和维度统计误标为必须实现。

## 4. 数据库和事件验收

- [x] `short_link_event` 表通过 Goose migration 创建。
- [x] `short_link_event.short_link_id` 引用 `short_link.id`。
- [x] `short_link_event.event_type` 记录稳定事件名。
- [x] `short_link_event.created_at` 可用于今日统计和最近访问时间。
- [x] 成功访问短链写入 `redirect_response_sent` 事件。
- [x] 禁用短链访问不计入访问量。
- [x] 不存在或软删除短链访问不计入访问量。

## 5. API 验收

- [x] 我的短链列表返回 `stats.visitCount`。
- [x] 我的短链列表返回 `stats.todayVisitCount`。
- [x] 我的短链列表返回 `stats.lastVisitedAt`。
- [x] 管理员全站短链列表返回同样统计字段。
- [x] 无访问事件时，总访问量和今日访问量为 `0`。
- [x] 无访问事件时，最近访问时间为空。
- [x] 既有分页、状态筛选和管理员关键词搜索不回退。

## 6. 控制台验收

- [x] 我的短链列表展示总访问量。
- [x] 我的短链列表展示今日访问量。
- [x] 我的短链列表展示最近访问时间。
- [x] 全站短链列表展示总访问量。
- [x] 全站短链列表展示今日访问量。
- [x] 全站短链列表展示最近访问时间。
- [x] 无最近访问时展示「暂无访问」或等价文案。
- [x] 移动端短链列表不出现横向溢出。

## 7. 既有能力回归验收

- [x] 用户可以创建短链。
- [x] 用户可以查看自己的短链列表。
- [x] 用户可以按状态筛选自己的短链。
- [x] 管理员可以查看全站短链。
- [x] 管理员可以按状态和关键词筛选全站短链。
- [x] 用户可以启用、禁用和删除自己的短链。
- [x] 管理员可以启用、禁用和删除全站短链。
- [x] 有效短链可以跳转。
- [x] 禁用短链不会跳转。
- [x] 软删除短链按不存在处理。

## 8. 暂不实现项确认

以下能力不应作为 v0.0.4 验收阻塞项：

- [x] 不要求独立统计分析页。
- [x] 不要求短链详情页。
- [x] 不要求近 7 天趋势图。
- [x] 不要求近 30 天趋势图。
- [x] 不要求来源页面统计。
- [x] 不要求浏览器、操作系统和设备类型统计。
- [x] 不要求地区统计。
- [x] 不要求最近访问明细。
- [x] 不要求统计数据导出。
- [x] 不要求访问 IP、User-Agent 和 Referer 持久化。
- [x] 不要求访问密码。
- [x] 不要求短链过期时间。
- [x] 不要求二维码能力。
- [x] 不要求中间页跳转和确认页跳转。

## 9. 发布前检查

- [x] 所有自动化测试通过。
- [x] 构建通过。
- [x] 核心短链访问和列表统计验收完成。
- [x] 文档与实际功能一致。
- [x] 没有把 v0.1.0 能力误标为 v0.0.4 已完成。

## 10. 自动化验证记录

验证时间：2026-07-16 14:16-14:32，环境时区 `+08:00`。

已通过命令：

- `go test ./internal/db -run TestInitialMigrationCreatesCoreTablesAndConstraints -count=1`
- `go test ./internal/db -run TestShortLinkStatisticsQueries -count=1`
- `go test ./internal/event -count=1`
- `go test ./internal/shortlink -run TestRedirectServiceRecordsSuccessfulVisit -count=1`
- `go test ./internal/shortlink -run "TestServiceListReturnsOnlyOwnActiveRecords|TestServiceAdminListReturnsAllOwners" -count=1`
- `go test ./internal/shortlink -run TestHandlerListShortLinksReturnsItemsAndMeta -count=1`
- `go test ./internal/db ./internal/event ./internal/shortlink -count=1`
- `go test ./...`
- `go vet ./...`
- `go test ./internal/auth ./internal/db ./internal/event ./internal/http ./internal/middleware ./internal/permission ./internal/shortlink ./internal/system ./internal/user -coverprofile=coverage.out`
- 后端覆盖率门禁：`Go coverage: 100.00%`
- `cd web && pnpm lint`
- `cd web && pnpm typecheck`
- `cd web && pnpm test`
- `cd web && pnpm test:coverage`
- `cd web && pnpm build`
- `git diff --check`
- 移动端视口检查：`390px` 视口下 `/link` 页面 `bodyScrollWidth=390`、`docScrollWidth=390`。

补充记录：

- 前端命令在当前机器会提示 Node 版本警告：项目声明 `node 26.x`，当前运行环境为 `node v22.21.0`。该警告未阻断 `lint`、`typecheck`、单元测试、覆盖率和构建。
- `cd web && pnpm test:e2e` 已尝试执行，但 Docker Compose 构建阶段拉取基础镜像时，Docker Hub token 请求超时失败，测试服务未启动。该项记录为当前机器网络或镜像拉取环境阻塞，不作为业务失败结论。
