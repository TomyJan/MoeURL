# v0.3.0 任务级实施清单

> 未执行的项目不得标记为完成。

## 文档与范围

- [x] 完成 v0.3.0 受保护短链访问范围、功能设计和实现计划。
- [x] 同步文档中心、路线图、产品总览、技术基线和 `AGENTS.md` 的版本边界。

## 数据库与权限

- [x] 新增密码字段、失败窗口、密码更新时间和短期授权表 migration，并在独立 migration 中验证历史数据约束。
- [x] 验证升级、回滚、既有数据默认值和内置权限。
- [x] 更新 SQLC 查询和生成代码，确认无手工漂移。

## 后端访问

- [x] 创建、编辑、清除密码并通过权限服务按当前用户的 `GroupKey` 检查 `permission.ShortLinkSetPassword`，同时执行长度校验。
- [x] 实现数据库行锁失败限流和短期授权 token。
- [x] Open、Preview、Continue 统一检查密码条件且不污染统计口径。
- [x] 解锁错误映射和事件写入失败不阻断公开访问流程。

## 前端体验

- [x] 创建和设置表单按权限展示密码配置，并在创建请求 pending 时锁定高级控件。
- [x] 公共密码页覆盖加载、错误、限流和成功状态。
- [x] 公开预览仅校验允许的短码、目标主机名、倒计时和过期时间字段，Vite 开发服务器精确代理 `/go/{slug}/preview`、`/go/{slug}/unlock` 和 `/go/{slug}/continue` 数据路由，`/go/{slug}` 仍由 Vite App Shell 处理。
- [x] 中英文文案、移动端布局和无障碍状态完整。

## 发布验收

- [x] Go 测试、vet、业务覆盖率通过。
- [x] Vitest、覆盖率、lint、typecheck、build 通过。
- [x] Playwright E2E 通过真实 UI 覆盖密码创建、保留、修改、清除、受保护中间页、授权、失效和统计口径。
- [x] 更新验收记录并创建本地原子提交。
