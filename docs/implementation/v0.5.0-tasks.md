# v0.5.0 任务级实施清单

> 未执行的项目不得标记为完成。

## 文档与范围

- [x] 完成 v0.5.0 用户组权限管理范围、功能设计和实现计划。
- [x] 同步文档中心、路线图、产品总览、稳定规格和技术基线。

## 权限目录与数据访问

- [x] 建立覆盖全部当前权限常量的服务端目录、分类、保护规则和三个预设。
- [x] 固定 `guest` 为空权限和只读状态，避免在访客资源归属未实现时开放假能力。
- [x] 增加内置用户组列表与 `updated_at` 条件更新 SQLC 查询，验证稳定排序、并发冲突和生成结果零漂移。
- [x] 保证未知、重复权限和受保护权限归属变更不能写入数据库。

## 后端管理 API

- [x] 实现用户组管理 Service，复用数据库权限 Resolver 并对一次操作只解析一份权限快照。
- [x] 实现用户组列表和权限更新 API，覆盖统一业务错误码、HTTP `500` 和日志边界。
- [x] 完成 Router、App 生产装配和运行时撤权验证，不允许缺少 Resolver 时放行。

## 前端体验

- [x] 将 `/admin/user/group` 替换为真实页面，并移除导航中的规划中状态。
- [x] 实现三个内置组 Tabs、`guest` 只读状态、`user`/`admin` 独立权限草稿、分类复选框、只读保护权限和三个预设。
- [x] 实现加载、空数据、读取失败、保存失败、并发冲突和成功反馈，并在保存后刷新用户组与 `auth/me`。
- [x] 补齐中英文文案、键完整性测试和桌面/移动响应式布局。

## 发布验收

- [x] Go 测试、vet、业务覆盖率、Docstring Coverage 和 SQLC 零漂移通过。
- [ ] Vitest、前端覆盖率、lint、typecheck 和生产构建已在 Node.js 22.21.0 本地通过；Node.js 26.x CI 待验证。
- [ ] Playwright E2E 已在 Node.js 22.21.0 本地从真实页面覆盖权限保存、普通用户能力即时变化、基线恢复和双视口布局；Node.js 26.x CI 待验证。
- [x] 更新验收记录并创建本地原子提交。
