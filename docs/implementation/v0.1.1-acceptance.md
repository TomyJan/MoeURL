# v0.1.1 验收清单

> 未执行的项目不得标记为完成。

## API 与权限

- [x] 个人概览 API 只返回当前用户拥有且未删除短链的聚合结果。
- [x] API 使用 `short_link:read_own`，管理员个人概览不泄露全站数据。
- [x] 总数、启用数、总访问量和今日访问量口径正确。
- [x] 无数据返回零值，权限不足和基础设施错误符合既有响应约定。

## 控制台

- [x] `/console` 展示真实概览数据，不展示伪造数据。
- [x] 最近 5 条短链按创建时间倒序展示，并可进入短链管理和访问分析。
- [x] 指标与最近短链具备独立加载和错误状态。
- [x] 空数据、移动端和中英文文案完整。
- [x] 概览导航不再标记为规划中，旧概览占位配置已删除。

## 发布验证

- [x] `go test ./...` 通过。
- [x] `go vet ./...` 通过。
- [x] 后端覆盖率门禁为 100%。
- [x] `cd web && pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm test:coverage`、`pnpm build` 通过。
- [x] `git diff --check` 通过。

## 验证记录

验证时间：2026-07-30，环境时区 `+08:00`。

验证环境：

- Go `1.25.7`，Windows `amd64`。
- Node.js `22.21.0`；仓库要求 Node.js 26，pnpm 命令显示 engine 警告但均成功退出。
- 项目 pnpm `11.5.0`。
- Docker `29.5.2`，`desktop-linux` context。

已通过：

- `go test -p=1 ./internal/auth ./internal/db ./internal/event ./internal/http ./internal/middleware ./internal/permission ./internal/shortlink ./internal/system ./internal/user -coverprofile=coverage.out -count=1`。
- `node scripts/go-coverage-threshold.mjs coverage.out 100 --include-from=scripts/go-coverage-targets.txt --exclude-blocks-from=scripts/go-coverage-excluded-blocks.txt`，结果 `Go coverage: 100.00%`。
- `go test ./... -count=1`。
- `go vet ./...`。
- `cd web && pnpm lint`。
- `cd web && pnpm typecheck`。
- `cd web && pnpm test`，结果 25 个测试文件、169 项测试通过。
- `cd web && pnpm test:coverage`，语句、分支、函数和行覆盖率均为 100%。
- `cd web && pnpm build`。

验证说明：

- 首次将 `go test ./...` 与其他门禁并行执行时，多个 Testcontainers 包同时初始化 Docker provider，`internal/auth` 出现一次 Windows rootless provider 瞬时错误；`docker context show`、隔离复现和随后单独执行完整命令均通过，未修改 Docker 配置。
- 独立代码审查发现最近短链未展示设计要求的创建时间；已补齐共享短链响应的 `createdAt`、SQLC 映射、双语页面展示及有效和异常日期测试。
- 本机未安装 SQLC 命令，在线获取 SQLC v1.30.0 时受 IPv6 网络阻断；已从本地 Go 模块缓存离线构建同版本生成器并成功重新生成查询代码。
- Vite 构建保留现有大 chunk 警告，不影响构建成功；本版本未新增依赖或扩大该问题。
- `cd web && pnpm test:e2e` 已尝试执行，但 Compose 在拉取 `node:26-alpine` 的 Docker Hub 认证 token 时连接到不可达 IPv6 地址，WebServer 启动前失败，未进入 Playwright 测试用例；本清单未将 E2E 标记为通过。
