# v0.1.2 任务级实施清单

> 未执行的项目不得标记为完成。

## 1. 文档与范围

- [x] 新增 v0.1.2 范围、设计和实施计划文档。
- [x] 更新 `docs/README.md`、`docs/product/overview.md` 和 `docs/product/roadmap.md`。
- [x] 明确个人设置页、自助昵称修改、偏好复用和非目标边界。
- [x] 修正文档中心的阅读顺序、当前优先目标和实施入口。

## 2. 后端自助昵称更新

- [x] 新增 SQLC 查询 `UpdateAppUserNickname`。
- [x] 在 `internal/user/model.go`、`internal/user/service.go` 和 `internal/user/handler.go` 中增加 `UpdateProfile` 数据流。
- [x] 注册 `POST /api/v1/user/profile/update`。
- [x] 覆盖权限不足、空白昵称、内置账号、缺失用户、数据库错误和路由测试。
- [x] 运行 `go test ./internal/user ./internal/http ./internal/db -count=1`。
- [x] 运行 `go test ./...` 与 `go vet ./...`。

## 3. 前端个人设置页

- [x] 新增 `/profile` 页面并复用 `auth/me` 与 `PreferenceSwitcher`。
- [x] 新增 `updateProfile` API。
- [x] 接入 `/profile` 路由守卫。
- [x] 让首页账号入口跳转到个人设置页。
- [x] 让控制台桌面端和移动端账号入口都能进入个人设置页。
- [x] 添加个人设置页、路由、API 和入口测试。
- [x] 运行 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm test:e2e`、`pnpm test:coverage` 和 `pnpm build`。

## 4. 收尾

- [x] 补齐实施层验收清单。
- [x] 复核实现只覆盖个人设置、自助昵称修改和入口整合，不扩展头像、密码、系统设置或服务端偏好同步。
