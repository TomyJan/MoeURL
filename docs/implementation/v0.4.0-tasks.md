# v0.4.0 任务级实施清单

> 未执行的项目不得标记为完成。

## 文档与范围

- [x] 完成 v0.4.0 确认页访问范围、功能设计和实现计划。
- [x] 同步文档中心、路线图、产品总览、稳定规格、技术基线和 `AGENTS.md` 的版本边界。

## 数据库与权限

- [ ] 扩展跳转模式约束和内置 `short_link:use_confirmation` 权限，保证 migration 可升级和回滚。
- [ ] 验证既有数据保持、confirmation 回滚降级、权限可逆和 SQLC 查询兼容。

## 后端访问

- [ ] 创建和更新按目标模式动态检查权限，并允许无高级模式权限时切回 direct。
- [ ] Open、Preview、Continue 支持 confirmation 及密码组合，公开预览显式返回 `redirectMode`。
- [ ] confirmation 点击只产生非统计事件，最终访问量继续只统计 `redirect_response_sent`。

## 前端体验

- [ ] 创建和设置表单按权限组合展示 direct、intermediate、confirmation。
- [ ] 列表正确展示确认页模式，公开预览严格校验第三种模式。
- [ ] 公共确认页不自动跳转，覆盖密码解锁后确认、失败重试、中英文和移动布局。

## 发布验收

- [ ] Go 测试、vet、业务覆盖率和 SQLC 零漂移通过。
- [ ] Vitest、覆盖率、lint、typecheck、build 通过。
- [ ] Playwright E2E 通过真实 UI 覆盖确认页、密码组合、二次检查、统计口径和双视口布局。
- [ ] 更新验收记录并创建本地原子提交。

