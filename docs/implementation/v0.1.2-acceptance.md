# v0.1.2 验收清单

> 未执行的项目不得标记为完成。

## API 与权限

- [x] `POST /api/v1/user/profile/update` 只允许当前登录用户修改自己的昵称。
- [x] `guest`、空白昵称、内置账号、缺失用户和数据库错误都按既有响应约定处理。
- [x] 接口返回更新后的当前用户摘要，可直接回填 `auth/me` 缓存。

## 页面与导航

- [x] `/profile` 可以展示当前账号信息、昵称编辑区、语言和主题偏好入口。
- [x] 首页账号入口和控制台账号入口都可以进入个人设置页。
- [x] 个人设置页在加载、局部错误、保存成功、移动端布局和空状态下可用。
- [x] 页面文案、路由和导航入口都使用稳定的中英文翻译键。

## 发布验证

- [x] `go test ./...` 通过。
- [x] `go vet ./...` 通过。
- [x] `cd web && pnpm lint` 通过。
- [x] `cd web && pnpm typecheck` 通过。
- [x] `cd web && pnpm test` 通过。
- [x] `cd web && pnpm test:e2e` 通过。
- [x] `cd web && pnpm test:coverage` 通过。
- [x] `cd web && pnpm build` 通过。

## 验证记录

验证时间：2026-08-02，环境时区 `Asia/Singapore`。

验证环境：

- Go `1.25.x`。
- Node.js `26.3.0`，pnpm `11.5.0`。与 `web/package.json` 的 `engines.node = 26.x` 一致。

已通过：

- `go test ./internal/user ./internal/http ./internal/db -count=1`。
- `go test ./...`。
- `go vet ./...`。
- `cd web && pnpm test -- src/entities/user/api.test.ts src/pages/ProfilePage.test.ts src/pages/pages.test.ts src/app/router.test.ts src/widgets/console-shell/ConsoleShell.test.ts src/widgets/console-shell/ConsoleAccountCard.test.ts src/app/i18n.test.ts`。
- `cd web && pnpm lint`。
- `cd web && pnpm typecheck`。
- `cd web && pnpm test`。
- `cd web && pnpm test:e2e`。
- `cd web && pnpm test:coverage`，最终覆盖率 `100%`。
- `cd web && pnpm build`。

验证说明：

- `pnpm test:coverage` 曾在 `ProfilePage.vue` 的分支覆盖率上停在 `99.69%`；已通过补充分支测试并统一主显示名逻辑修复，最终覆盖率回到 `100%`。
- 所有命令均在当前分支 `feature/v0.1.2-user-center-settings` 上执行。
