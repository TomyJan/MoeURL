# v0.1.0 验收清单

> 未执行的项目不得标记为完成。

## 数据与隐私

- [x] migration 可创建和回滚事件维度字段。
- [x] 只保存来源主机名、设备类型和可选国家代码。
- [x] 不保存原始 IP、完整 User-Agent 或完整 Referer。
- [x] 仅成功写出跳转响应后写入统计事件。
- [x] recorder 满载或失败不会阻断跳转。

## API 与权限

- [x] 用户统计 API 只返回当前用户短链。
- [x] 管理员统计 API 可返回任意未删除短链。
- [x] 概览、连续 7 天趋势和三类维度聚合口径正确。
- [x] 无效 ID、权限不足、短链不存在和基础设施错误符合既有响应约定。

## 控制台

- [x] `/analytics` 展示真实短链数据，不展示伪造数据。
- [x] 两个短链列表可进入对应分析页。
- [x] 页面具备加载、空数据、错误和移动端状态。
- [x] 中英文文案完整，分析导航不再标记为规划中。

## 发布验证

- [x] `go test ./...` 通过。
- [x] `go vet ./...` 通过。
- [x] 后端覆盖率门禁为 100%。
- [x] `cd web && pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm test:coverage`、`pnpm build` 通过。
- [ ] `cd web && pnpm test:e2e` 在 CI 通过。
- [x] `git diff --check` 通过。

## 验证记录

验证时间：2026-07-17，环境时区 `+08:00`。

已通过：

- `go test -p=1 ./internal/auth ./internal/db ./internal/event ./internal/http ./internal/middleware ./internal/permission ./internal/shortlink ./internal/system ./internal/user -coverprofile=coverage.out -count=1 -v`
- `node scripts/go-coverage-threshold.mjs coverage.out 100 --include-from=scripts/go-coverage-targets.txt --exclude-blocks-from=scripts/go-coverage-excluded-blocks.txt`，结果 `Go coverage: 100.00%`。
- `go test ./... -count=1`。
- `go vet ./...`。
- `cd web && pnpm lint && pnpm typecheck && pnpm test && pnpm test:coverage && pnpm build`。
- `git diff --check`。

待 CI 验证：

- 本机 `pnpm test:e2e` 在 Docker Compose 构建阶段卡在镜像内安装 Goose 的依赖下载，未生成测试服务或 Playwright 报告。`E2E` GitHub Actions job 已新增，负责使用相同的 Compose 流程完成该发布门禁。
