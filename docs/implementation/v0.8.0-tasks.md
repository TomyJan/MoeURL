# v0.8.0 多域名管理任务清单

状态：功能实现与分阶段本地验证已有记录，当前提交的容器集成门禁待复验；目标环境验收待证据。

## 数据与权限

- [x] 新增 `00013` 迁移、授权回填、权限变更跟踪以及 Up/Down/再次 Up 测试。
- [x] 新初始化默认域名同时授权内置 `user`、`admin`。
- [x] 增加 `domain:use_assigned` 与受保护的 `domain:manage`，同步目录快照和双语标签。
- [x] 用固定版本 SQLC 生成域名及短链查询，并验证生成一致性。

## 后端闭环

- [x] 实现根 Origin 校验、旧裸 authority 兼容和同义地址冲突。
- [x] 实现管理 Service/API、组授权、事务与 `expectedUpdatedAt` 冲突。
- [x] 创建时按 `domainId`、权限和组授权共同决定目标域名。
- [x] 已有短链 URL 从其保存的域名构造，默认切换不改写旧链接。
- [x] 全部公开路径按真实 Host 和域名启用状态重新校验。

## 前端闭环

- [x] 完成严格域名 API schema、管理页面、删除确认、脏草稿和冲突恢复。
- [x] 完成创建选择器、缓存失效与失权回退。
- [x] 完成双语、导航和管理固定路由。
- [x] 运行前端单元覆盖率、lint、类型检查与构建。

## 集成与发布

- [ ] 当前提交的隔离 Compose 双 Host E2E 待复验；此前通过与资源清理记录见验收清单。
- [x] 全量 Go 测试、vet、选定业务源码覆盖率、SQLC 零漂移与前端完整门禁复验。
- [ ] 当前提交的 Playwright 全量与 Compose smoke 待复验；此前通过记录见验收清单。
- [x] 完整 review 安全、权限、并发、迁移、前端状态及部署边界并完成本地提交。
- [ ] 目标 Node.js 26.x CI、Linux race、安全扫描、隔离恢复和真实 DNS/TLS 验收取得可追溯证据。
