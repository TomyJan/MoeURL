# v0.4.0 确认页访问详细实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 在保留 v0.3.0 密码授权和统计口径的前提下，实现 `confirmation` 跳转模式和公共确认页完整闭环。

**架构：** 复用 `short_link.redirect_mode`、`/go/{slug}` App Shell、同源预览和 continue 路由。后端按目标模式执行动态权限校验，公开预览显式返回模式，前端状态机在 intermediate 下倒计时、在 confirmation 下等待主动继续；确认点击不建立授权，也不计入访问量。

**技术栈：** Go 1.25、Chi、SQLC 1.30、Goose、PostgreSQL、Vue 3、TypeScript、Vuetify 4、TanStack Query、Vitest、Playwright。

---

## 文件边界

- 创建：`migrations/00008_short_link_confirmation.sql`、`migrations/00009_validate_short_link_confirmation.sql`、`web/e2e/v0.4.0-confirmation-access.spec.ts`。
- 修改：`queries/short_link.sql` 兼容性测试与 `internal/db/sqlc` 生成校验、`internal/permission/permission.go`、`internal/shortlink/{model,errors,service,redirect_service,redirect_handler}.go`、前端短链模型/API/创建/设置/列表/公共访问页面/i18n 及对应测试。
- 验证：Go 全量测试与覆盖率、SQLC 零漂移、Vitest 全量测试与覆盖率、lint、typecheck、build、Playwright E2E、`git diff --check`。

## 任务 1：数据库约束与权限

**文件：**

- 创建：`migrations/00008_short_link_confirmation.sql`
- 创建：`migrations/00009_validate_short_link_confirmation.sql`
- 修改：`internal/db/migration_test.go`
- 修改：`internal/db/sqlc_test.go`
- 修改：`internal/permission/permission.go`
- 修改：`internal/permission/service_test.go`

- [x] 在 `internal/db/migration_test.go` 先增加失败测试：迁移后允许写入 `confirmation`，既有 direct/intermediate 不变，`user`/`admin` 获得新权限而 `guest` 不获得；回滚后 confirmation 变为 direct，预先存在的同名权限不被删除。
- [x] 运行 `go test ./internal/db ./internal/permission -count=1`，确认因 migration 和权限常量缺失失败。
- [x] 实现 `00008`：替换 `short_link_redirect_mode_check` 为允许三种模式的 `NOT VALID` 约束，使用权限跟踪表追加 `short_link:use_confirmation`；Down 先降级 confirmation，再恢复旧约束并精确回滚权限。
- [x] 实现 `00009` 独立验证新约束；在 `permission.go` 增加 `ShortLinkUseConfirmation` 并加入内置 user/admin 权限。
- [x] 在 `internal/db/sqlc_test.go` 增加通过既有创建、读取、列表、更新查询往返 confirmation 的集成测试，确认无需改变查询字段。
- [x] 运行 `go test ./internal/db ./internal/permission -count=1`，预期通过。
- [x] 使用 SQLC `1.30.0` 执行生成并检查 `git diff --exit-code -- internal/db/sqlc`，预期无差异。
- [x] 提交：`feat(数据库): 扩展确认页模式与权限`。

## 任务 2：管理 API 模式契约

**文件：**

- 修改：`internal/shortlink/model.go`
- 修改：`internal/shortlink/service.go`
- 修改：`internal/shortlink/service_test.go`
- 修改：`internal/shortlink/service_internal_test.go`
- 修改：`internal/shortlink/handler_test.go`

- [x] 先增加失败测试：创建/更新 confirmation 成功；动态移除 `short_link:use_confirmation` 后拒绝；只有确认权限时不能创建 intermediate；失去高级权限后仍可切回 direct；仅修改倒计时仍要求 intermediate 权限；管理员路径使用当前 `GroupKey` 权限。
- [x] 运行 `go test ./internal/shortlink -run 'Confirmation|RedirectMode|AccessConfig' -count=1`，确认因第三种模式未允许和权限未检查失败。
- [x] 在 `model.go` 增加 `RedirectModeConfirmation = "confirmation"`。
- [x] 将 `service.go` 的模式校验拆为目标模式权限校验：direct 无额外权限，intermediate 检查 `ShortLinkUseIntermediate`，confirmation 检查 `ShortLinkUseConfirmation`；倒计时显式变更独立检查 intermediate 权限。
- [x] 保持创建默认 direct、更新省略字段保持原值、密码和过期逻辑不变。
- [x] 运行定向测试及 `go test ./internal/shortlink -count=1`，预期通过。
- [x] 提交：`feat(短链): 支持确认页模式配置`。

## 任务 3：公共访问状态机与事件

**文件：**

- 修改：`internal/event/event.go`
- 修改：`internal/event/recorder_test.go`
- 修改：`internal/shortlink/errors.go`
- 修改：`internal/shortlink/model.go`
- 修改：`internal/shortlink/redirect_service.go`
- 修改：`internal/shortlink/redirect_test.go`
- 修改：`internal/shortlink/redirect_service_internal_test.go`
- 修改：`internal/shortlink/redirect_handler.go`
- 修改：`internal/shortlink/redirect_handler_test.go`

- [x] 先增加失败测试：confirmation Open 进入 App Shell；Preview 返回显式 confirmation；无密码和有效密码授权均可 Continue；密码解锁后 Preview 保留 confirmation；禁用/过期/删除二次检查生效；无密码 direct 的 Preview/Continue 返回交互模式错误。
- [x] 增加事件测试：confirmation Continue 发出 `confirmation_clicked` 和 `redirect_initiated`，但 DB Recorder 忽略 confirmation 事件；Handler 只在最终 `302` 写出成功后记录 `redirect_response_sent`。
- [x] 运行 `go test ./internal/event ./internal/shortlink -run 'Confirmation|Preview|Continue' -count=1`，确认因状态机和响应字段缺失失败。
- [x] 在 `PreviewResult` 增加 `RedirectMode` JSON 字段；把“必须是 intermediate”校验泛化为“必须是 interactive”，允许 intermediate/confirmation 或任意受密码保护模式。
- [x] 扩展 `Open` 把 confirmation 路由到 `/go/{slug}`；扩展 `Continue` 允许 confirmation，并仅在 confirmation 下记录 `event.ConfirmationClicked`。
- [x] 将公开错误内部命名调整为 `ErrShortLinkNotInteractive`，保留业务码 `200110`，前端兼容同一数值错误码。
- [x] 运行 `go test ./internal/event ./internal/shortlink -count=1`，预期通过。
- [x] 提交：`feat(访问): 实现确认页访问状态机`。

## 任务 4：前端模式配置与确认页

**文件：**

- 修改：`web/src/entities/short-link/model.ts`
- 修改：`web/src/entities/short-link/api.ts`
- 修改：`web/src/entities/short-link/api.test.ts`
- 修改：`web/src/features/short-link-create/ShortLinkCreatePanel.vue`
- 修改：`web/src/features/short-link-create/ShortLinkCreatePanel.test.ts`
- 修改：`web/src/features/short-link-settings/ShortLinkSettingsDialog.vue`
- 修改：`web/src/features/short-link-settings/ShortLinkSettingsDialog.test.ts`
- 修改：`web/src/pages/ConsoleLinkList.vue`
- 修改：`web/src/pages/pages.test.ts`
- 修改：`web/src/pages/RedirectPage.vue`
- 修改：`web/src/pages/RedirectPage.test.ts`
- 修改：`web/src/app/i18n.ts`
- 修改：`web/src/app/i18n.test.ts`

- [x] 先更新实体/API 测试：`RedirectMode` 接受 confirmation，公开预览必须包含合法 `redirectMode`，拒绝缺失或未知模式且继续拒绝额外敏感字段。
- [x] 先增加创建/设置失败测试：模式选项按两项权限独立组合；confirmation 提交不携带无关倒计时；失去确认权限时不能提交 confirmation；切回 direct 正常。
- [x] 先增加页面失败测试：confirmation 不启动倒计时、不自动调用 `location.assign`，点击继续只导航一次；密码解锁后的 confirmation 停留等待点击；列表显示确认页标签；中英文键完整。
- [x] 使用 Node 26 运行相关 Vitest，确认因类型、schema、UI 状态缺失失败。
- [x] 扩展模型和严格 Zod schema；创建/设置组件以权限组合构建模式选项，并只在 intermediate 展示和提交倒计时。
- [x] 重构 `RedirectPage` 的 `proceedAfterAccess`：direct 立即继续、intermediate 启动倒计时、confirmation 保持静止并渲染确认文案。
- [x] 增加列表标签和中英文文案，复用现有公共页布局约束。
- [x] 运行相关 Vitest、`pnpm typecheck` 和 `pnpm lint`，预期通过。
- [x] 提交：`feat(前端): 添加确认页配置与访问体验`。

## 任务 5：E2E 与发布验收

**文件：**

- 创建：`web/e2e/v0.4.0-confirmation-access.spec.ts`
- 修改：`docs/implementation/v0.4.0-tasks.md`
- 修改：`docs/implementation/v0.4.0-acceptance.md`
- 修改：`docs/product/overview.md`
- 修改：`docs/README.md`
- 修改：`README.md`
- 修改：`AGENTS.md`

- [x] 新增 Playwright 用例，通过真实 UI 创建 confirmation，验证初次访问停留确认页、点击前访问量为 0、点击后目标 `302` 且访问量为 1。
- [x] 在同一隔离流程覆盖受密码保护 confirmation：错误密码不跳转，正确密码后仍停留确认页，主动点击后才继续。
- [x] 在继续前改变短链为禁用或过期，验证二次检查阻止跳转且访问量不增加。
- [x] 在 `1280 x 720` 与 `390 x 800` 验证确认页和设置对话框的控件顺序、操作区几何和横向溢出；不使用固定等待。
- [x] 运行 Go 全量测试、vet、后端 100% 覆盖率、SQLC 零漂移、前端 lint/typecheck/test/100% 覆盖率/build 和 Playwright E2E。
- [x] 更新产品总览、文档中心、根 README、`AGENTS.md`，把 v0.4.0 从实施中改为已完成；只勾选有命令证据的任务和验收项。
- [x] 运行 `git diff --check` 和 `git status --short --branch`，确认无验证缓存或临时文件。
- [x] 提交：`docs(v0.4.0): 完成确认页访问验收`。
