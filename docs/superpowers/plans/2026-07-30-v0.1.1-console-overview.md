# v0.1.1 控制台个人概览实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将 `/console` 从占位页替换为当前用户的真实概览，准确展示四项聚合指标和最近 5 条短链。

**架构：** PostgreSQL 通过一条 SQLC 查询聚合当前用户全部未删除短链及成功跳转事件，Go 服务使用 `short_link:read_own` 暴露个人概览 API。Vue 页面以两个独立 TanStack Query 并行加载概览指标和既有短链列表，使局部错误不遮蔽另一部分可用数据。

**技术栈：** Go 1.25、Chi、SQLC、PostgreSQL、Vue 3、TypeScript、Vuetify 4、TanStack Query、vue-i18n、Vitest。

---

## 文件结构

- 修改 `queries/short_link.sql`：定义当前所有者概览聚合查询。
- 修改 `internal/db/sqlc/short_link.sql.go`：由 SQLC 生成查询参数、结果和方法。
- 修改 `internal/db/sqlc_test.go`：集成验证聚合口径和所有者隔离。
- 修改 `internal/shortlink/model.go`：定义 JSON 概览结果。
- 修改 `internal/shortlink/service.go`：执行权限检查、解析所有者并映射 SQLC 结果。
- 修改 `internal/shortlink/handler.go`：扩展端口并返回统一概览响应。
- 修改 `internal/http/router.go`：注册固定概览 API 路由。
- 修改 `internal/shortlink/service_test.go`、`internal/shortlink/handler_test.go`、`internal/http/router_test.go`：覆盖服务、响应和路由行为。
- 修改 `web/src/entities/short-link/model.ts`：定义前端概览类型。
- 修改 `web/src/entities/short-link/api.ts`、`api.test.ts`：实现并测试概览请求。
- 创建 `web/src/pages/ConsoleOverviewPage.vue`：渲染真实概览、最近短链和局部状态。
- 修改 `web/src/pages/pages.test.ts`：覆盖概览页数据、状态、重试和导航。
- 修改 `web/src/app/router.ts`、`router.test.ts`：将 `/console` 切换到真实页面。
- 修改 `web/src/widgets/console-shell/ConsoleShell.vue`、`ConsoleShell.test.ts`：移除概览规划中徽标。
- 修改 `web/src/pages/ConsolePlaceholderPage.vue`：删除概览占位类型。
- 修改 `web/src/app/i18n.ts`、`i18n.test.ts`：删除旧占位文案并补齐概览双语文案。
- 修改 `docs/implementation/v0.1.1-tasks.md`、`v0.1.1-acceptance.md`：记录实际完成项与验证证据。

### 任务 1：当前用户概览 SQLC 聚合

**文件：**
- 修改：`internal/db/sqlc_test.go`
- 修改：`queries/short_link.sql`
- 生成：`internal/db/sqlc/short_link.sql.go`

- [ ] **步骤 1：编写失败的 SQL 集成测试**

在 `TestShortLinkOverviewQuery` 中创建 Alice 的启用、禁用、软删除短链和 Bob 的短链，并写入今日成功事件、历史成功事件和非成功事件。核心断言为：

```go
overview, err := queries.GetShortLinkOverviewByOwner(ctx, uuidToPgtype(ownerID))
if err != nil {
	t.Fatalf("get short link overview: %v", err)
}
if overview.TotalLinkCount != 2 || overview.ActiveLinkCount != 1 {
	t.Fatalf("unexpected link counts: %#v", overview)
}
if overview.VisitCount != 2 || overview.TodayVisitCount != 1 {
	t.Fatalf("unexpected visit counts: %#v", overview)
}
```

- [ ] **步骤 2：运行测试验证因查询缺失而失败**

运行：`go test ./internal/db -run TestShortLinkOverviewQuery -count=1`

预期：FAIL，`Queries` 没有 `GetShortLinkOverviewByOwner` 方法。

- [ ] **步骤 3：增加最小 SQLC 查询**

在 `queries/short_link.sql` 中新增：

```sql
-- name: GetShortLinkOverviewByOwner :one
select count(distinct short_link.id)::bigint as total_link_count,
    count(distinct short_link.id) filter (where short_link.status = 'active')::bigint as active_link_count,
    count(short_link_event.id) filter (where short_link_event.event_type = 'redirect_response_sent')::bigint as visit_count,
    count(short_link_event.id) filter (
        where short_link_event.event_type = 'redirect_response_sent'
            and short_link_event.created_at >= current_date
            and short_link_event.created_at < current_date + interval '1 day'
    )::bigint as today_visit_count
from short_link
left join short_link_event on short_link_event.short_link_id = short_link.id
where short_link.owner_id = $1
    and short_link.deleted_at is null;
```

- [ ] **步骤 4：生成 SQLC 并验证绿灯**

本机没有全局 `sqlc`，运行：

```powershell
$env:GOPROXY='https://goproxy.cn,direct'
go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.30.0 generate
go test ./internal/db -run TestShortLinkOverviewQuery -count=1
```

预期：PASS，生成代码只更新 `internal/db/sqlc/short_link.sql.go`。

- [ ] **步骤 5：提交聚合查询**

```powershell
git add queries/short_link.sql internal/db/sqlc/short_link.sql.go internal/db/sqlc_test.go
git commit -m "feat(统计): 新增个人概览聚合查询"
```

### 任务 2：个人概览服务与 HTTP API

**文件：**
- 修改：`internal/shortlink/model.go`
- 修改：`internal/shortlink/service.go`
- 修改：`internal/shortlink/service_test.go`
- 修改：`internal/shortlink/handler.go`
- 修改：`internal/shortlink/handler_test.go`
- 修改：`internal/http/router.go`
- 修改：`internal/http/router_test.go`

- [ ] **步骤 1：编写失败的服务测试**

添加 `TestServiceOverviewReturnsOnlyOwnAggregates`、`TestServiceOverviewRejectsMissingPermission` 和数据库关闭错误断言。公开 API 形状固定为：

```go
result, err := service.Overview(ctx, user)
if err != nil {
	t.Fatalf("get overview: %v", err)
}
if result.TotalLinkCount != 2 || result.ActiveLinkCount != 1 || result.VisitCount != 2 || result.TodayVisitCount != 1 {
	t.Fatalf("unexpected overview: %#v", result)
}
```

- [ ] **步骤 2：运行服务测试验证红灯**

运行：`go test ./internal/shortlink -run TestServiceOverview -count=1`

预期：FAIL，`Service` 没有 `Overview` 方法或 `OverviewResult` 类型。

- [ ] **步骤 3：实现最小服务逻辑**

在 `model.go` 定义：

```go
type OverviewResult struct {
	TotalLinkCount  int64 `json:"totalLinkCount"`
	ActiveLinkCount int64 `json:"activeLinkCount"`
	VisitCount      int64 `json:"visitCount"`
	TodayVisitCount int64 `json:"todayVisitCount"`
}
```

在 `service.go` 先检查 `permission.ShortLinkReadOwn`，再解析 `user.ID`，调用 `GetShortLinkOverviewByOwner` 并逐字段映射；查询错误原样返回。

- [ ] **步骤 4：验证服务绿灯**

运行：`go test ./internal/shortlink -run TestServiceOverview -count=1`

预期：PASS。

- [ ] **步骤 5：编写失败的 Handler 与路由测试**

扩展 `fakeShortLinkService`，添加成功响应断言：

```go
request := httptest.NewRequest(http.MethodGet, "/api/v1/short-link/overview", nil)
router.ServeHTTP(response, request)
assertBusinessCode(t, response, http.StatusOK, 0)
```

同时断言 `ErrPermissionDenied` 映射为 HTTP `200` / `120001`，普通错误映射为 HTTP `500` / `900000`，且路由只接受 `GET`。

- [ ] **步骤 6：运行 Handler 测试验证红灯**

运行：`go test ./internal/shortlink ./internal/http -run "Overview|ShortLinkRoutes" -count=1`

预期：FAIL，端口和路由尚未提供概览方法。

- [ ] **步骤 7：实现最小 Handler 与路由**

在 `Port` 增加：

```go
Overview(ctx context.Context, user auth.CurrentUser) (OverviewResult, error)
```

Handler 调用服务并复用 `writeBusinessOrSystemError` 与 `ok`；路由注册：

```go
api.Get("/short-link/overview", shortLinkHandler.Overview)
```

- [ ] **步骤 8：验证后端模块绿灯并提交**

运行：`go test ./internal/shortlink ./internal/http -count=1`

预期：PASS。

```powershell
git add internal/shortlink internal/http/router.go internal/http/router_test.go
git commit -m "feat(API): 提供个人短链概览"
```

### 任务 3：前端概览 API 契约

**文件：**
- 修改：`web/src/entities/short-link/model.ts`
- 修改：`web/src/entities/short-link/api.ts`
- 修改：`web/src/entities/short-link/api.test.ts`

- [ ] **步骤 1：编写失败的 API 测试**

```typescript
it('loads the current user overview', async () => {
  vi.mocked(apiGet).mockResolvedValueOnce({
    data: { totalLinkCount: 12, activeLinkCount: 9, visitCount: 840, todayVisitCount: 31 },
    meta: {},
  })

  await expect(getShortLinkOverview()).resolves.toEqual({
    totalLinkCount: 12,
    activeLinkCount: 9,
    visitCount: 840,
    todayVisitCount: 31,
  })
  expect(apiGet).toHaveBeenCalledWith('/short-link/overview')
})
```

- [ ] **步骤 2：运行测试验证红灯**

运行：`cd web; pnpm test -- src/entities/short-link/api.test.ts`

预期：FAIL，缺少 `getShortLinkOverview` 导出。

- [ ] **步骤 3：实现最小类型和请求函数**

```typescript
export interface ShortLinkOverview {
  totalLinkCount: number
  activeLinkCount: number
  visitCount: number
  todayVisitCount: number
}

export async function getShortLinkOverview(): Promise<ShortLinkOverview> {
  const response = await apiGet<ShortLinkOverview>('/short-link/overview')
  return response.data
}
```

- [ ] **步骤 4：验证绿灯并提交**

运行：`cd web; pnpm test -- src/entities/short-link/api.test.ts`

预期：PASS。

```powershell
git add web/src/entities/short-link/model.ts web/src/entities/short-link/api.ts web/src/entities/short-link/api.test.ts
git commit -m "feat(前端): 接入个人概览 API"
```

### 任务 4：真实控制台概览页

**文件：**
- 创建：`web/src/pages/ConsoleOverviewPage.vue`
- 修改：`web/src/pages/pages.test.ts`
- 修改：`web/src/app/router.ts`
- 修改：`web/src/app/router.test.ts`
- 修改：`web/src/widgets/console-shell/ConsoleShell.vue`
- 修改：`web/src/widgets/console-shell/ConsoleShell.test.ts`
- 修改：`web/src/pages/ConsolePlaceholderPage.vue`
- 修改：`web/src/app/i18n.ts`
- 修改：`web/src/app/i18n.test.ts`

- [ ] **步骤 1：编写失败的页面成功态测试**

调整 `pages.test.ts` 的 Query mock，使连续两次 `useQuery` 可返回独立结果。添加概览成功态，断言四项数值、最近短码、两个入口，并执行保存的 query functions 后确认：

```typescript
expect(getShortLinkOverview).toHaveBeenCalledTimes(1)
expect(listShortLinks).toHaveBeenCalledWith({ page: 1, pageSize: 5 })
expect(screen.getByText('840')).toBeTruthy()
expect(screen.getByText('abc123')).toBeTruthy()
expect(screen.getByRole('link', { name: 'overview.viewAllLinks' })).toBeTruthy()
```

- [ ] **步骤 2：运行页面测试验证红灯**

运行：`cd web; pnpm test -- src/pages/pages.test.ts`

预期：FAIL，`ConsoleOverviewPage.vue` 尚不存在。

- [ ] **步骤 3：实现最小成功页**

创建页面并使用：

```typescript
const overviewQuery = useQuery({
  queryKey: ['short-links', 'overview'],
  queryFn: getShortLinkOverview,
})
const recentLinksQuery = useQuery({
  queryKey: ['short-links', 'recent'],
  queryFn: () => listShortLinks({ page: 1, pageSize: 5 }),
})
```

模板用固定四列指标带和最近短链行展示真实数据，入口分别指向 `/link` 和 `/analytics?shortLinkId=<id>`。

- [ ] **步骤 4：验证成功态绿灯**

运行：`cd web; pnpm test -- src/pages/pages.test.ts`

预期：PASS。

- [ ] **步骤 5：编写失败的局部状态测试**

分别为两个 Query 提供 `isPending`、`isError` 和空结果，断言 skeleton 尺寸稳定、错误区域有独立重试按钮、另一部分仍显示、空状态不含第二套表单。

- [ ] **步骤 6：运行测试验证局部状态红灯**

运行：`cd web; pnpm test -- src/pages/pages.test.ts`

预期：FAIL，缺少局部 loading、error、retry 或 empty 状态。

- [ ] **步骤 7：实现局部状态和响应式样式**

两个错误按钮分别调用：

```typescript
function retryOverview() {
  void overviewQuery.refetch()
}

function retryRecentLinks() {
  void recentLinksQuery.refetch()
}
```

桌面端指标使用四列稳定网格，窄屏改为两列和单列；长 URL 使用换行或省略，按钮不随内容改变尺寸。

- [ ] **步骤 8：编写并验证路由、导航和 i18n 红灯**

测试要求：`/console` 组件为 `ConsoleOverviewPage`；普通用户只剩概览之外的规划徽标；占位类型仅允许 `userGroups | settings`；中英文 message tree 对齐且无 `placeholder.overview`。

运行：

```powershell
cd web
pnpm test -- src/app/router.test.ts src/widgets/console-shell/ConsoleShell.test.ts src/app/i18n.test.ts
```

预期：FAIL，现有路由、徽标和文案仍指向占位实现。

- [ ] **步骤 9：切换真实路由并清理占位内容**

- `router.ts` 导入并挂载 `ConsoleOverviewPage`。
- `ConsoleShell.vue` 将概览项改为 `{ labelKey: 'nav.overview', to: '/console' }`。
- `ConsolePlaceholderPage.vue` 删除 `overview` 类型和 metadata。
- `i18n.ts` 删除 `placeholder.overview`，新增 `overview` 下标题、指标、最近短链、空态、错误、重试和入口文案。

- [ ] **步骤 10：验证前端相关测试并提交**

运行：

```powershell
cd web
pnpm test -- src/pages/pages.test.ts src/app/router.test.ts src/widgets/console-shell/ConsoleShell.test.ts src/app/i18n.test.ts
```

预期：PASS。

```powershell
git add web/src/pages web/src/app/router.ts web/src/app/router.test.ts web/src/app/i18n.ts web/src/app/i18n.test.ts web/src/widgets/console-shell
git commit -m "feat(控制台): 交付个人概览工作台"
```

### 任务 5：发布验证与验收记录

**文件：**
- 修改：`docs/implementation/v0.1.1-tasks.md`
- 修改：`docs/implementation/v0.1.1-acceptance.md`

- [ ] **步骤 1：运行后端完整门禁**

```powershell
$env:GOPROXY='https://goproxy.cn,direct'
go test -p=1 ./internal/auth ./internal/db ./internal/event ./internal/http ./internal/middleware ./internal/permission ./internal/shortlink ./internal/system ./internal/user -coverprofile=coverage.out -count=1
node scripts/go-coverage-threshold.mjs coverage.out 100 --include-from=scripts/go-coverage-targets.txt --exclude-blocks-from=scripts/go-coverage-excluded-blocks.txt
go test ./... -count=1
go vet ./...
```

预期：全部退出码为 `0`，覆盖率脚本输出 `Go coverage: 100.00%`。

- [ ] **步骤 2：运行前端完整门禁**

```powershell
cd web
pnpm lint
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm build
```

预期：全部退出码为 `0`。本机 Node.js 22 与仓库要求的 Node.js 26 不一致时，记录环境警告，但任何命令失败仍必须修复或如实报告。

- [ ] **步骤 3：检查变更和需求覆盖**

```powershell
git diff --check
git status --short
git diff 1c0973e --stat
```

逐项核对 `scope-v0.1.1.md` 和设计规格；确认无系统级仪表盘、伪造统计或新建表单越界。

- [ ] **步骤 4：更新验收文档**

仅将有新鲜验证证据的任务和验收项改为 `[x]`，在验证记录中填写日期、环境、实际命令和结果；无法执行的 E2E 不得标记为通过。

- [ ] **步骤 5：请求中文代码审查并修复重要问题**

审查范围：`git diff 1c0973e..HEAD` 加上未提交验收记录。重点检查权限隔离、SQL 聚合重复计数、局部错误、响应式布局和文档一致性。所有 `[必须修复]` 和 `[建议修改]` 问题在最终提交前处理。

- [ ] **步骤 6：提交验收记录**

```powershell
git add docs/implementation/v0.1.1-tasks.md docs/implementation/v0.1.1-acceptance.md
git commit -m "docs(v0.1.1): 记录控制台概览验收结果"
```

- [ ] **步骤 7：最终复核**

运行：`git status --short --branch; git log --oneline -6`

预期：工作区干净，分支为 `feat/v0-1-1-console-overview`，所有 v0.1.1 提交可见且未推送。
