# v0.1.0 发布一致性收尾实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。本次按用户要求在当前仓库功能分支执行，不创建 worktree。

**目标：** 让根 README 和控制台占位文案准确反映已经完成的 v0.1.0，并清理分析页上线后遗留的无效占位配置。

**架构：** 保持现有路由、统计页面和产品范围不变。前端仅收窄 `ConsolePlaceholderPage` 支持的占位页种类并同步移除对应双语消息；文档仅更新面向使用者的版本、能力、技术栈和验证入口。

**技术栈：** Vue 3、TypeScript、vue-i18n、Vitest、Markdown。

---

## 文件结构

- 修改：`web/src/app/i18n.test.ts`，锁定已完成能力不得继续出现在占位消息中，且占位文案不得绑定历史版本号。
- 修改：`web/src/app/i18n.ts`，删除无调用方的分析占位消息，并将设置页说明改为版本无关表述。
- 修改：`web/src/pages/ConsolePlaceholderPage.vue`，删除不再可达的 `analytics` 占位种类和元数据。
- 修改：`README.md`，同步 v0.1.0 当前状态、统计能力、Vuetify 4、当前文档入口和后端覆盖率命令。
- 修改：`Dockerfile`，让容器安装的 Goose 版本与 `go.mod` 和裸机运行说明一致。
- 检查并按需修改：`docs/README.md`、相关 `docs/product/` 产品文档和 `docs/implementation/` 技术文档；版本范围变更时必须与根 `README.md` 同步更新这些文档。

### 任务 1：清理过期控制台占位配置

- [x] **步骤 1：编写失败的 i18n 回归测试**

在 `web/src/app/i18n.test.ts` 增加断言，要求双语概览占位项不再包含 `analytics`，并要求当前占位消息不出现历史版本号：

```typescript
it('keeps placeholder copy limited to capabilities that are still planned', () => {
  expect(messages['zh-CN'].placeholder.overview.items).not.toHaveProperty('analytics')
  expect(messages.en.placeholder.overview.items).not.toHaveProperty('analytics')
  expect(JSON.stringify(messages['zh-CN'].placeholder)).not.toMatch(/v\d+\.\d+\.\d+/)
  expect(JSON.stringify(messages.en.placeholder)).not.toMatch(/v\d+\.\d+\.\d+/)
})
```

- [x] **步骤 2：运行测试验证红灯**

运行：`cd web && pnpm test -- src/app/i18n.test.ts`

预期：FAIL，指出中英文 `placeholder.overview.items` 仍包含 `analytics`，或占位文案仍包含历史版本号。

- [x] **步骤 3：编写最少实现**

在 `web/src/app/i18n.ts` 删除中英文 `placeholder` 下的 `analytics` 分支，并将设置页说明改为：

```typescript
description:
  '系统设置页暂不开放表单；域名仍通过初始化或部署配置维护，主题偏好由前端控制，并保留跟随系统、浅色和深色模式切换。'
```

```typescript
description:
  'The settings page does not expose forms yet. Domains remain managed through setup or deployment configuration, while theme preferences stay in the frontend with system, light, and dark modes.'
```

在 `web/src/pages/ConsolePlaceholderPage.vue` 将类型收窄为：

```typescript
type PlaceholderKind = 'overview' | 'userGroups' | 'settings'
```

并删除 `metadata.analytics`。

- [x] **步骤 4：运行测试验证绿灯**

运行：`cd web && pnpm test -- src/app/i18n.test.ts src/pages/pages.test.ts`

预期：PASS。

### 任务 2：同步根 README

- [x] **步骤 1：更新版本与功能概览**

修改 `README.md`：

- 当前开发重点改为 v0.1.0 统计分析闭环已完成。
- 功能概览补充列表统计、近 7 天趋势、来源、设备和地区聚合。
- 前端技术栈更正为 Vuetify 4。
- 文档入口切换到 v0.1.0 范围、统计规格、计划和验收清单。

- [x] **步骤 2：同步质量检查命令**

将后端覆盖率包列表补齐 `internal/event` 和 `internal/middleware`，与 v0.1.0 验收记录保持一致。

- [x] **步骤 3：同步容器迁移工具版本**

将 `Dockerfile` 中安装的 Goose 从 `v3.26.0` 更新到 `go.mod` 使用的 `v3.27.3`，避免裸机说明、构建依赖和生产镜像使用不同版本。

- [x] **步骤 4：扫描过期内容**

扫描根 `README.md`、`Dockerfile`、前端文件、`docs/README.md`、相关产品文档与技术文档，检查旧版本信息、旧 Vuetify 信息和过期能力说明：

运行：

```powershell
rg -n "当前开发重点是 v0[.]0[.]3|Vuetify [3]|placeholder[.]analytics|误以为 v0[.]0[.]3|goose@v3[.]26[.]0" Dockerfile README.md web/src docs/README.md docs/product/overview.md docs/product/scope-v0.1.0.md docs/implementation/technical-decision.md docs/implementation/technical-baseline.md docs/implementation/v0.1.0-plan.md docs/implementation/v0.1.0-tasks.md docs/implementation/v0.1.0-acceptance.md docs/superpowers/plans/2026-07-30-v0.1.0-release-consistency.md
```

预期：无匹配，`rg` 退出码为 `1`。

### 任务 3：验证与提交

- [x] **步骤 1：运行前端质量检查**

运行：`cd web && pnpm lint && pnpm typecheck && pnpm test && pnpm test:coverage && pnpm build`

预期：全部退出码为 `0`，覆盖率门禁通过。

- [x] **步骤 2：运行后端回归与差异检查**

运行：

```powershell
go test ./...
go vet ./...
git diff --check
```

预期：全部退出码为 `0`。

- [x] **步骤 3：审查并提交**

检查 `git diff --stat`、`git diff` 和 `git status --short`，确认只包含计划内文件后提交：

```powershell
git add Dockerfile README.md docs/superpowers/plans/2026-07-30-v0.1.0-release-consistency.md web/src/app/i18n.test.ts web/src/app/i18n.ts web/src/pages/ConsolePlaceholderPage.vue
git commit -m "fix(发布): 同步 v0.1.0 项目状态"
```
